[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live_worker: requires real Celery workers instead of eager mode
    docker_integration: requires docker compose and live postgres/redis
    postgres_integration: requires a live PostgreSQL database
